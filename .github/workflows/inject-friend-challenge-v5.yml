name: Legacy Friends Challenge Injection (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy injection workflow disabled; friends challenge is loaded by aiux.js/admin-access-v2.js.'
