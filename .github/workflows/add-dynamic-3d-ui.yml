name: Add Dynamic Responsive Layout and 3D UI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ui:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled automatic source mutation
        run: echo 'Automatic UI mutation disabled; canonical Android build is source of truth.'
