import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';

const cors = {'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS'};
const MODEL = Deno.env.get('GEMINI_MODEL') || 'gemini-3.5-flash-lite';
const GEMINI_URL = `https://generativelanguage.googleapis.com/v1beta/models/${MODEL}:generateContent`;
const questionSchema = {type:'object',properties:{stem:{type:'string'},options:{type:'array',items:{type:'string'},minItems:4,maxItems:4},correct_option:{type:'integer',minimum:0,maximum:3},explanation:{type:'string'},common_mistake:{type:'string'},difficulty:{type:'string',enum:['easy','medium','hard','elite']},cognitive_level:{type:'string'},estimated_seconds:{type:'integer',minimum:20,maximum:600},unit:{type:'string'},lesson:{type:'string'},concept:{type:'string'},skill:{type:'string'},source_basis:{type:'string'}},required:['stem','options','correct_option','explanation','common_mistake','difficulty','cognitive_level','estimated_seconds','unit','lesson','concept','skill','source_basis']};
const generationSchema = {type:'object',properties:{questions:{type:'array',items:questionSchema}},required:['questions']};
const reviewSchema = {type:'object',properties:{approved:{type:'boolean'},score:{type:'integer',minimum:0,maximum:100},issues:{type:'array',items:{type:'string'}}},required:['approved','score','issues']};
function fingerprint(q:any){return [q.stem,...(q.options||[])].join(' ').toLowerCase().normalize('NFKC').replace(/[^\p{L}\p{N}]+/gu,' ').trim();}
function validateQuestion(q:any){if(!q||typeof q.stem!=='string'||q.stem.trim().length<15)return false;if(!Array.isArray(q.options)||q.options.length!==4)return false;if(q.options.some((x:any)=>typeof x!=='string'||x.trim().length<2))return false;if(new Set(q.options.map((x:string)=>x.trim().toLowerCase())).size!==4)return false;if(![0,1,2,3].includes(q.correct_option))return false;if(typeof q.explanation!=='string'||q.explanation.trim().length<20)return false;if(typeof q.common_mistake!=='string'||q.common_mistake.trim().length<10)return false;return true;}
function jsonResponse(body:unknown,status=200){return new Response(JSON.stringify(body),{status,headers:{...cors,'Content-Type':'application/json'}});}
async function geminiJson(prompt:string,schema:any,key:string){const ai=await fetch(GEMINI_URL,{method:'POST',headers:{'x-goog-api-key':key,'Content-Type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:prompt}]}],generationConfig:{response_mime_type:'application/json',response_schema:schema,temperature:0.35}})});const raw=await ai.text();if(!ai.ok)throw new Error(raw.slice(0,2000));let response:any;try{response=JSON.parse(raw);}catch{throw new Error('Invalid Gemini response JSON');}const outputText=response?.candidates?.[0]?.content?.parts?.map((p:any)=>p?.text||'').join('').trim();if(!outputText)throw new Error(JSON.stringify(response?.promptFeedback||response?.candidates?.[0]?.finishReason||'No text returned'));try{return JSON.parse(outputText);}catch{throw new Error('Invalid structured output from Gemini');}}
async function main(req:Request){if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return jsonResponse({error:'POST only'},405);const geminiKey=Deno.env.get('GEMINI_API_KEY')||Deno.env.get('GOOGLE_API_KEY');const supabaseUrl=Deno.env.get('SUPABASE_URL');const serviceKey=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY');if(!geminiKey||!supabaseUrl||!serviceKey)return jsonResponse({error:'AI service is not configured. Add GEMINI_API_KEY to Supabase Secrets.'},500);let body:any;try{body=await req.json();}catch{return jsonResponse({error:'Invalid JSON body'},400);}const subject=String(body.subject||'').trim();const branch=String(body.branch||'').trim()||null;const unit=String(body.unit||'').trim()||null;const lesson=String(body.lesson||'').trim()||null;const difficulty=String(body.difficulty||'medium');const count=Math.min(Math.max(Number(body.count||10),1),20);const userId=body.user_id||null;if(!subject)return jsonResponse({error:'subject is required'},400);const sb=createClient(supabaseUrl,serviceKey);const {data:sources}=await sb.from('ai_source_documents').select('id,title,source_type,subject,grade,branch,school_year,url,content').eq('is_active',true).or(`subject.eq.${subject},subject.eq.عام`).limit(30);const {data:existing}=await sb.from('ai_generated_questions').select('stem,options,fingerprint').eq('subject',subject).eq('grade','الصف الثالث الثانوي').eq('status','approved').order('created_at',{ascending:false}).limit(300);const sourceContext=(sources||[]).map((s:any)=>({title:s.title,type:s.source_type,year:s.school_year,branch:s.branch,url:s.url,content:s.content?s.content.slice(0,12000):null}));const avoid=(existing||[]).slice(0,120).map((q:any)=>q.stem).join('\n- ');const instructions=`أنت محرك أسئلة احترافي للصف الثالث الثانوي المصري. أنشئ أسئلة MCQ أصلية مبنية على المنهج والمراجع المتاحة فقط.\nقواعد إلزامية: لا تخترع معلومة أو قاعدة. لا تستخدم محتوى خارج المنهج. لكل سؤال إجابة صحيحة واحدة فقط. كل مشتت يجب أن يكون منطقيًا ومبنيًا على خطأ شائع.\nفي النحو والبلاغة والأدب: تحقق من القاعدة المقصودة بدقة، ولا تخلط بين المحسنات اللفظية والمعنوية، ولا بين المدارس الأدبية وخصائصها، ولا بين شروط الإعمال.\nلا تنسب اقتباسًا حرفيًا لمصدر ما لم يكن النص موجودًا في المراجع. إذا لم يتوفر النص، استخدم مثالًا تطبيقيًا جديدًا دون نسبة.\nاكتب عربية سليمة، بلا أخطاء إملائية أو نحوية. اجعل السؤال واضحًا ومناسبًا لنمط الثانوية العامة المصرية. نوّع مستويات التفكير والصعوبة. أعد JSON فقط.`;const userInput={request:{subject,grade:'الصف الثالث الثانوي',branch,unit,lesson,difficulty,count},reference_sources:sourceContext,already_generated_to_avoid:avoid};const run=await sb.from('ai_generation_runs').insert({user_id:userId,subject,grade:'الصف الثالث الثانوي',branch,unit,lesson,difficulty,count_requested:count,model:MODEL}).select('id').single();const runId=run.data?.id||null;let parsed:any;try{parsed=await geminiJson(`${instructions}\n\nبيانات الطلب والمراجع:\n${JSON.stringify(userInput,null,2)}`,generationSchema,geminiKey);}catch(e){if(runId)await sb.from('ai_generation_runs').update({status:'failed',error_message:String(e).slice(0,2000)}).eq('id',runId);return jsonResponse({error:'Gemini generation failed',detail:String(e).slice(0,2000)},502);}const clean=(parsed.questions||[]).filter(validateQuestion);const seen=new Set<string>();const candidates=clean.filter((q:any)=>{const fp=fingerprint(q);if(seen.has(fp))return false;seen.add(fp);return true;}).slice(0,count);const approved:any[]=[];const rejected:any[]=[];for(const q of candidates){const reviewPrompt=`أنت لجنة مراجعة جودة صارمة لأسئلة الثانوية العامة المصرية. راجع السؤال التالي مراجعة موضوعية مقارنة بالمراجع المرفقة.\nيجب رفض السؤال إذا كان هناك أي شك جوهري في صحة المعلومة أو القاعدة، أو إجابة صحيحة ثانية، أو لبس، أو خطأ لغوي مؤثر، أو شرح لا يطابق الإجابة، أو نسبة غير موثوقة، أو محتوى خارج المنهج.\nفي أسئلة النحو والبلاغة والأدب، افحص المصطلح والقاعدة والاختيار الصحيح تحديدًا، ولا تعتمد على الانطباع العام. إذا كان السؤال يحتاج نصًا مقررًا غير موجود في المراجع، ارفضه.\nاقبل فقط إذا كان السؤال صالحًا مباشرة للاستخدام في امتحان ثانوية عامة. score >= 95 شرط للقبول. اذكر سبب الرفض باختصار عند عدم القبول.\nالمراجع:\n${JSON.stringify(sourceContext,null,2)}\n\nالسؤال:\n${JSON.stringify(q,null,2)}\n\nأعد JSON فقط.`;try{const review=await geminiJson(reviewPrompt,reviewSchema,geminiKey);if(review?.approved===true&&Number(review.score)>=95)approved.push({...q,quality_score:Number(review.score)});else rejected.push({stem:q.stem,issues:review?.issues||['رفض المراجع']});}catch{rejected.push({stem:q.stem,issues:['تعذر إتمام مراجعة الجودة']});}}
for(const q of approved){await sb.from('ai_generated_questions').insert({subject,grade:'الصف الثالث الثانوي',branch,unit:q.unit||unit,lesson:q.lesson||lesson,concept:q.concept,skill:q.skill,question_type:'mcq',difficulty:q.difficulty,cognitive_level:q.cognitive_level,stem:q.stem,options:q.options,correct_option:q.correct_option,explanation:q.explanation,common_mistake:q.common_mistake,estimated_seconds:q.estimated_seconds,fingerprint:fingerprint(q),quality_score:q.quality_score,status:'approved',model:MODEL,generation_run_id:runId});}if(runId)await sb.from('ai_generation_runs').update({status:'completed',count_generated:approved.length}).eq('id',runId);return jsonResponse({questions:approved,model:MODEL,generated:approved.length,candidates:candidates.length,rejected:rejected.length,quality_gate:'strict-review'});}
Deno.serve(main);
